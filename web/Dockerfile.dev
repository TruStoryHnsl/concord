FROM node:22-alpine
WORKDIR /app
COPY client/package.json client/package-lock.json ./
RUN npm ci
# Source is bind-mounted at runtime for live editing
EXPOSE 5173
CMD ["npx", "vite", "--host", "0.0.0.0", "--port", "5173"]
